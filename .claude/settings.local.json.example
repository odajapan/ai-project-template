{
  "$comment": "Personal overrides for Claude Code. Copy to settings.local.json (gitignored).",
  "env": {
    "ANTHROPIC_API_KEY": "sk-ant-..."
  },
  "permissions": {
    "allow": [
      "Bash(open:*)"
    ]
  }
}
