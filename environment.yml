name: your_project_name
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      # Minimal install: project only.
      # After activating, add dev tools and extras with the canonical set:
      #   pip install -e ".[dev,claude,jira]"
      #   # or: make requirements
      - "-e ."
