name: your_project_name
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      # Core dependencies only for CI / minimal runtime
      # For full dev environment, use: pip install -r requirements.txt
      # or: pip install -e .[dev,notebook,viz,docs,cloud]
      - "-e ."

