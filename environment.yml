name: your_project_name
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
	  # Core dependencies only for CI / minimal runtime
	  # For development, install extras after activating the environment, e.g.:
	  #   pip install -r requirements.txt        # dev-only (lightweight default)
	  #   # or: pip install -e .[dev,notebook,viz,docs,cloud]
	  - "-e ."

