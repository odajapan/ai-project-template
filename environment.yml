name: your_project_name
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      # Minimal install: project only.
      # After activating, add dev tools and extras with:
      #   pip install -e .[dev]
      #   # or: pip install -e .[dev,claude,notebook,viz,docs,cloud]
      - "-e ."
