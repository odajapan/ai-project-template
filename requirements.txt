#! /usr/bin/env pip

# Convenience installer for this project.
#
# All dependency definitions live in pyproject.toml (single source of truth).
#
# Default: install the project in editable mode with the canonical extras
# (dev + claude + jira — same set as make requirements and CI; see AGENTS.md).

-e .[dev,claude,jira]

# Example for projects that also use notebooks, visualization, docs, cloud
# tools, and dashboards:
# -e .[dev,claude,jira,notebook,viz,docs,cloud,dashboard]
